apiVersion: v1
kind: Service
metadata:
  name: db
spec:
  selector:
    app: db        # finds pods with this label
  ports:
    - port: 3306          # port other services use to reach this one
      targetPort: 3306    # port the container actually listens on
  type: ClusterIP         # internal only — use NodePort to expose outside