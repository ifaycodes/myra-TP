apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: myra-pipeline
spec:
  selector:
    app: redis        # finds pods with this label
  ports:
    - port: 6379          # port other services use to reach this one
      targetPort: 6379    # port the container actually listens on
  type: ClusterIP         